FUNCTION "FN_RETURN_USER"( 
    IV_USER NVARCHAR(256)
 )
    RETURNS TABLE(
        USER_NAME NVARCHAR(256)
    )
    LANGUAGE SQLSCRIPT
    SQL SECURITY INVOKER AS
BEGIN
    rt_username = SELECT :IV_USER AS USER_NAME FROM DUMMY;
    RETURN :rt_username;
END;